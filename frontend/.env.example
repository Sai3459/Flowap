# Base URL of the NestJS backend. Vite only exposes vars prefixed with VITE_ to the
# client, and the backend already runs with CORS enabled, so no dev proxy is needed.
VITE_API_BASE_URL=http://localhost:3000
